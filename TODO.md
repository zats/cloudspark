# TODO

- Delete Workers Builds API integration from the library client.
- Delete Workers Builds models and merged build/version history types.
- Delete old CLI subcommands and keep a single root command.
- Delete CLI options and help text that mention in-progress or failed builds.
- Delete README claims that build history is supported today.
- Restore Builds support only after Cloudflare exposes a usable permission/token path for this account.
